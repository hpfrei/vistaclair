node_modules/
.env
data/
interactions/
outputs/
docs/api/

# Pro extensions (separate repos)
vistaclair-pro/
vistaclair-apps/

# MCP config — contains auth tokens, auto-generated per machine
.mcp.json
.mcp.json.bak

# Claude settings
.claude/

# Capabilities — ignore runtime state and secrets, track definitions
capabilities/secrets.json
capabilities/secrets.enc
.secrets_key
capabilities/active.json
capabilities/workflows/*/runs/

# MCP servers — track tool definitions, ignore runtime
mcp-servers/*/node_modules/
mcp-servers/*/logs/
mcp-servers/*/package-lock.json
mcp-servers/*/workflows/
mcp-servers/blank/

# Sessions — live auth state (session hashes, IPs, user agents)
sessions.json
mcp-broker/

# Spend — track policy definitions, ignore accumulating ledgers
spend-ledger/*.json
