[
  {
    "id": "spend-guard",
    "name": "Spend Guard",
    "slug": "Stops a vistaclair-pro ai.prompt session mid-run once its app's or user's spend cap is exhausted (marker file per instanceId written by Pro's spend gate). Sessions without a marker are untouched.",
    "enabled": true,
    "builtin": true
  },
  {
    "id": "ai-tool-gate",
    "name": "AI Tool Gate",
    "slug": "Enforces ai.prompt allowedTools per session: strips ungranted tool definitions from the request (marker file per instanceId; [] = no tools at all). Runs first so later rules see the gated set.",
    "enabled": true,
    "builtin": true
  },
  {
    "id": "auq-mcp-rewrite",
    "name": "AskUserQuestion MCP Rewrite",
    "slug": "Rewrites AskUserQuestion ↔ vista-AskUserQuestion between CLI and API for the MCP tool handler",
    "enabled": true,
    "builtin": true
  },
  {
    "id": "unsafe-tool-filter",
    "name": "Tool Filter",
    "slug": "Strips dangerous or unwanted tools from the API request before they reach the LLM",
    "enabled": true,
    "builtin": true
  },
  {
    "id": "model-override",
    "name": "Model Override",
    "slug": "Global model override — rewrites claude-opus-4-7 → claude-opus-4-6. Runs after per-tab overrides (tab settings take priority).",
    "enabled": false,
    "builtin": true
  },
  {
    "id": "auth-inject",
    "name": "Auth Inject",
    "slug": "Injects OAuth token from ~/.claude/.credentials.json into requests missing auth headers, allowing --bare with Max/Pro subscriptions",
    "enabled": false,
    "builtin": true
  },
  {
    "id": "strip-git-status",
    "name": "Strip Git Status",
    "slug": "Removes the gitStatus block (and everything after it) from system prompt messages — strips branch, status, and recent commits info injected by Claude Code",
    "enabled": true,
    "builtin": true
  },
  {
    "id": "title-schema-shortcut",
    "name": "Title Schema Shortcut",
    "slug": "Short-circuits requests where output_config.format is a json_schema with type 'object', a single required property 'title', and additionalProperties: false. Instead of calling the LLM, returns a dummy response with text '{\"title\":\"Dummy (by vistaclair)\"}' and zero token usage. This avoids spending tokens on Claude Code's automatic title-generation requests while keeping claude -p compatibility intact.",
    "enabled": false,
    "builtin": true
  },
  {
    "id": "rule-req_mpww",
    "name": "content replacer",
    "slug": "Replaces every occurrence of \"mitm\" with \"superproxy\" across the system prompt, message text, tool inputs, and tool results in the request.",
    "enabled": false
  }
]
